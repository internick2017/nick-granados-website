---
deployment:
  tasks:
    - export DEPLOYDIR=/home1/nickda77/public_html
    - /bin/cp -R out/* $DEPLOYDIR/
    - find $DEPLOYDIR -type d -exec chmod 755 {} \;
    - find $DEPLOYDIR -type f -exec chmod 644 {} \;
